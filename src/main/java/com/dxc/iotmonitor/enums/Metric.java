package com.dxc.iotmonitor.enums;

// On entity field: @Enumerated(EnumType.STRING)
public enum Metric {
    TRAFFIC_DENSITY,
    AVG_SPEED,
    CO,
    OZONE,
    BRIGHTNESS_LEVEL,
    POWER_CONSUMPTION
}
